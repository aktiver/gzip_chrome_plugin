{
  "manifest_version": 3,
  "name": "GZIP Folder Compression",
  "version": "1.0",
  "description": "Select a folder and compress it as a single .tar.gz archive",
  "permissions": ["downloads"],
  "action": {
    "default_popup": "popup.html"
  }
}
